{
  "name": "output-sanitizer",
  "version": "1.0.0",
  "description": "Deterministic safety gate for untrusted model output",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
